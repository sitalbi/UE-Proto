# Quartz - Unreal Engine 5 gameplay prototype project
Action game prototype in Unreal Engine 5


## TODO

- think about attack direction:
  when not locked on, we always attack towards the input direction/ when locked on, we might need to attack towards the locked on target except if the input vector direction is > threshold in another direction.
